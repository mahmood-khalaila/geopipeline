apiVersion: v1
kind: Service

metadata:
  name: postgis

spec:
  selector:
    app: postgis

  ports:
    - port: {{ .Values.postgis.port }}
      targetPort: {{ .Values.postgis.port }}

  type: ClusterIP